---
title: Account Setup
description:  .
---

import { AccountSetup } from '@docs/components/blocks';

<ExternalLinks
  links={[
    
  ]}
/>

<div className="h-5" />

<div id="preview" className="scroll-m-20 *:hidden" aria-hidden>
  ## Preview
</div>

<BlockPreviewCard
  preview={
    <div className="w-full max-w-sm">
      <AccountSetup />
    </div>
  }
/>

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger id="cli" value="cli">CLI</TabsTrigger>
    <TabsTrigger id="manual" value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <CommandTabs args={["add", "account-setup"]}/>
  </TabsContent>
  <TabsContent value="manual">
    <Steps> 
      <Step>
        **If missing, add the following component:**

        - [`Button`](/docs/components/example-component)

      </Step>

      <Step>

        **Copy and paste the following code into your project.**

        ```json doc-gen:file
        {
          "file": "./node_modules/@native-blocks/library/src/blocks/account-setup.tsx",
          "codeblock": {
            "lang": "tsx",
            "meta": "title=\"@/components/blocks/account-setup.tsx\""
          }
        }
        ```

      </Step>

      <Step>

        **Update the import paths to match your project setup.**

      </Step>
    </Steps>
  </TabsContent>
</Tabs>

### Usage

```tsx
import { View } from 'react-native';
import { AccountSetup } from '@/blocks/account-setup';

export default function AccountSetupScreen() {
    return (
        <View>
            <AccountSetup />;
        </View>
    )
}
```
